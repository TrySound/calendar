// @flow

import type { Constraints, Year, Month, Day } from './types.js';

const getRangeOfYears = (constraints: Constraints): Year[] => {};

const getMonthsOfYear = (constraints: Constraints, date: Date): Month[] => {};

const getDaysOfMonth = (constraints: Constraints, date: Date): Day[] => {};

const getDaysOfNextMonth = (constraints: Constraints, date: Date): Day[] => {};

const getDaysOfPrevMonth = (constraints: Constraints, date: Date): Day[] => {};
